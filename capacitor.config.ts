import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.docecanto.coral',
  appName: 'Coral Doce Canto',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
